# 纸嫁衣4 (com.funnygames.zjy4.aligames) 资源解包指南

## 游戏基本信息

| 项目 | 内容 |
|------|------|
| 游戏名称 | 纸嫁衣4 |
| 包名 | com.funnygames.zjy4.aligames |
| 渠道 | 阿里游戏 |
| 游戏引擎 | Cocos Creator |
| 打包框架 | Apache Cordova（WebView 混合应用） |
| 平台 | Android |

## 引擎判断依据

判定本游戏使用 Cocos Creator 引擎开发，基于以下特征：

1. **资源目录结构**：manifest 清单解压后，资源路径遵循 Cocos Creator 标准的 Bundle 目录规范：
   ```
   assets/b0/config.json          # Bundle 配置
   assets/b0/import/...           # 序列化资源（JSON）
   assets/b0/native/...           # 原始资源（图片、音频）
   assets/b0/index.js             # Bundle 入口脚本
   assets/main/...                # main Bundle
   assets/internal/...            # internal Bundle（引擎内置资源）
   assets/resources/...           # resources Bundle（动态加载资源）
   ```
   这套 `config.json` + `import/` + `native/` + `index.js` 的 Bundle 结构是 Cocos Creator 3.x 的标志性特征。

2. **资源池（Pool）机制**：`assets/pool/` 目录采用内容寻址存储（hash 分目录），这是 Cocos Creator 在 Android 平台的资源打包方式，用于绕过 APK assets 目录的文件数量和路径长度限制。

3. **资源文件命名规则**：`import/` 下的 JSON 文件使用短 hash 命名（如 `0b09bb04d.json`、`03fa6321d.json`），`native/` 下的资源使用 UUID 命名（如 `749fc12d-b471-4bbf-bca2-fd19807d292c.png`），两者均为 Cocos Creator 的资源管线标准格式。

4. **Cordova + WebView 架构**：`assets/www/` 下存在 `cordova.js` 和 `index.html`，说明游戏通过 Cordova 框架将 Cocos Creator 的 Web 版本打包为原生 Android 应用，由 WebView 渲染运行。

## APK 目录结构

```
com.funnygames.zjy4.aligames/
├── AndroidManifest.xml          # Android 清单
├── classes.dex                  # Java 字节码
├── lib/                         # 原生 .so 库（arm64-v8a, armeabi-v7a, x86, x86_64）
├── res/                         # Android 资源
├── resources.arsc               # 编译后的资源索引
├── META-INF/                    # 签名信息
├── assets/
│   ├── www/                     # Cordova Web 层
│   │   ├── index.html           # 入口页面
│   │   ├── cordova.js           # Cordova 框架
│   │   ├── cordova_plugins.js   # 插件注册
│   │   └── plugins/             # Cordova 插件
│   ├── pool/                    # ★ 游戏核心资源（Cocos Creator 资源池）
│   │   ├── manifest/res/        # 资源清单（LZMA 压缩）
│   │   └── pool/                # 资源文件（按 hash 分目录存放）
│   ├── na.czl                   # 二进制资源包（自定义格式）
│   ├── nd                       # 二进制资源包（自定义格式）
│   └── ...                      # 第三方 SDK 资源（广告、支付等）
```

## 资源存储机制

Cocos Creator 在打包时采用 **内容寻址存储（Content-Addressable Storage）**：

- 所有资源文件按内容 hash 重命名为 16 位十六进制字符串
- 按 hash 前两位字符分两级目录存放，例如 `pool/7/f/7fc13035b2081437`
- 部分文件使用 LZMA 压缩，后缀为 `.lz`
- `manifest/res/` 下的清单文件记录了原始文件名与 hash 的映射关系

### 资源统计

| 类型 | 数量 | 说明 |
|------|------|------|
| WEBP 图片 | 949 | 游戏场景、UI、道具、角色等 |
| MP3 音频 | 537 | 背景音乐、音效 |
| LZ 压缩文件 | 57 | 内含 WAV 音频、JS 脚本、JSON 配置等 |

## 解包步骤

### 第一步：解压 APK

将 `.apk` 文件后缀改为 `.zip`，用任意解压工具解压即可。

```bash
# 或使用 apktool（可反编译 AndroidManifest.xml 和 resources.arsc）
apktool d com.funnygames.zjy4.aligames.apk -o com.funnygames.zjy4.aligames
```

### 第二步：解压 Manifest 清单

`assets/pool/manifest/res/` 下有两个 `.lz` 文件，使用 LZMA 格式压缩。

```python
import lzma, os

manifest_dir = "assets/pool/manifest/res"
for fname in os.listdir(manifest_dir):
    if not fname.endswith(".lz"):
        continue
    filepath = os.path.join(manifest_dir, fname)
    with open(filepath, "rb") as f:
        raw = f.read()
    decompressed = lzma.decompress(raw, format=lzma.FORMAT_ALONE)
    out_path = os.path.join(manifest_dir, fname.replace(".lz", ""))
    with open(out_path, "wb") as f:
        f.write(decompressed)
    print(f"已解压: {fname} -> {len(decompressed)} bytes")
```

解压后得到两个清单文件：

| 清单文件 | 内容 |
|----------|------|
| `4ec476556382b60a` | Cordova Web 层资源索引（Alert.html, fcm.css 等） |
| `04d33522dca0e6dc` | Cocos Creator 游戏资源索引（场景、图片、音频等，共 1563 条） |

#### 清单二进制格式

```
[文件头 11 字节]
[条目1][条目2]...[条目N]

每条记录格式：
  ┌─────────────┬──────────┬──────────┬────────────┬──────────┐
  │ flags (4B)  │ marker   │ name_len │ name       │ hash     │
  │             │ (1B)     │ (1B)     │ (变长)     │ (8B)     │
  └─────────────┴──────────┴──────────┴────────────┴──────────┘

- flags:    4 字节，含资源大小等元信息
- marker:   1 字节，标记符（0x20/0x21/0x26/0x2A）
- name_len: 1 字节，文件名长度
- name:     UTF-8 编码的原始资源路径
- hash:     8 字节，资源内容标识
```

> **注意**：清单中的 8 字节 hash 与 pool 目录下的 16 位 hex 文件名之间的映射算法
> 尚未完全逆向。如需还原原始文件名，需要进一步分析 Cocos Creator 引擎的
> pool 寻址逻辑（位于 `.lz` 压缩的 JS 引擎代码中）。

### 第三步：提取图片资源

pool 中的资源文件本身就是原始格式，无需额外解码，直接按文件头识别类型并复制即可。

```python
import os, shutil

POOL_DIR = "assets/pool/pool"
OUTPUT_DIR = "extracted_images"
os.makedirs(OUTPUT_DIR, exist_ok=True)

count = 0
for root, dirs, files in os.walk(POOL_DIR):
    for f in files:
        if f.endswith(".lz"):
            continue
        fp = os.path.join(root, f)
        with open(fp, "rb") as fh:
            header = fh.read(16)
        # 检测 WEBP 格式：RIFF....WEBP
        if header[:4] == b"RIFF" and header[8:12] == b"WEBP":
            shutil.copy2(fp, os.path.join(OUTPUT_DIR, f + ".webp"))
            count += 1

print(f"共提取 {count} 张 WEBP 图片")
```

运行结果：提取 **949 张 WEBP 图片**，总计约 **76.2 MB**。

### 第四步（可选）：解压 .lz 资源文件

pool 中 57 个 `.lz` 文件使用 LZMA 压缩，内含 WAV 音频、JS 脚本和 JSON 配置等。

```python
import lzma, os

POOL_DIR = "assets/pool/pool"
OUTPUT_DIR = "extracted_lz"
os.makedirs(OUTPUT_DIR, exist_ok=True)

for root, dirs, files in os.walk(POOL_DIR):
    for f in files:
        if not f.endswith(".lz"):
            continue
        fp = os.path.join(root, f)
        with open(fp, "rb") as fh:
            raw = fh.read()
        try:
            dec = lzma.decompress(raw, format=lzma.FORMAT_ALONE)
        except Exception as e:
            print(f"解压失败: {f} - {e}")
            continue
        # 根据内容自动加后缀
        if dec[:4] == b"RIFF" and dec[8:12] == b"WAVE":
            ext = ".wav"
        elif dec[:1] == b"{" or dec[:1] == b"[":
            ext = ".json"
        elif dec[:15].find(b"<!DOCTYPE") >= 0 or dec[:6].find(b"<html") >= 0:
            ext = ".html"
        elif dec[:10].find(b"function") >= 0 or dec[:10].find(b"window") >= 0 or dec[:10].find(b"var ") >= 0:
            ext = ".js"
        else:
            ext = ".bin"
        base = f.replace(".lz", "")
        with open(os.path.join(OUTPUT_DIR, base + ext), "wb") as fh:
            fh.write(dec)

print("LZ 文件解压完成")
```

### 第五步（可选）：WEBP 转 PNG

```bash
pip install Pillow
```

```python
from PIL import Image
import os, glob

for f in glob.glob("extracted_images/*.webp"):
    img = Image.open(f)
    img.save(f.replace(".webp", ".png"), "PNG")

print("WEBP -> PNG 转换完成")
```

## 常用工具

| 工具 | 用途 |
|------|------|
| apktool | APK 反编译（含 XML 和资源表解码） |
| Python + lzma | 解压 LZMA (.lz) 压缩文件 |
| Python + Pillow | 图片格式转换（WEBP → PNG） |
| file 命令 | 识别未知文件的实际格式 |

## 已知限制

1. **文件名丢失**：提取的资源以 hash 命名，无法直接对应游戏内的原始资源名。manifest 中虽记录了原始路径，但 hash 映射算法未完全逆向。
2. **`na.czl` / `nd` 文件**：自定义二进制格式，未做深入分析，可能包含额外资源。
3. **部分资源可能在线加载**：作为 Cordova 应用，部分资源可能从服务器动态下载，不在 APK 中。
